/**
 * Хэрэглэгч тутамд зөвшөөрөгдөх идэвхтэй төхөөрөмжийн дээд тоо.
 *
 * Тусад нь (server-only БИШ) файлд байгаа шалтгаан: `src/lib/api/devices.ts`
 * (сервер тал) БОЛОН `Protected.tsx` шиг клиент компонент (дэлгэц дээр "3
 * хүртэл" гэж харуулах) хоёулаа ижил тоог хэрэглэнэ.
 */
export const MAX_DEVICES = 3;
