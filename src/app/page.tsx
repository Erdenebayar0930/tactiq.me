import { redirect } from "next/navigation";

/**
 * НҮҮР ЗАМ (`/`) — ШУУД ХИЧЭЭЛИЙН ХЭСЭГ РҮҮ.
 *
 * ⚠ Урьд нь энд ТАНИЛЦУУЛГА сайт гардаг байсан (одоо `/about`). Тэр нь
 * шинэ хүнд нэг зогсоол нэмж байв: «сайт нээх → уншиж ойлгох →
 * бүртгүүлэх → дараа нь л хичээл харах». Хүүхэд тэр гинжнээс салдаг.
 *
 * Одоо сайт нээмэгц КУРС, ХИЧЭЭЛҮҮД харагдана. Нэвтрээгүй хүн (зочин)
 * замыг бүтнээр хараад эхний дасгалуудыг хийж чадна
 * (`components/tactiq/Protected.tsx`-ийн зочны горим), дасгал дуусахад
 * нэвтрэх хэсэг рүү уригдана.
 *
 * ⚠ `redirect` нь СЕРВЕР талд (307) — клиент талын `router.replace` БИШ:
 * тэгвэл хэрэглэгч эхлээд хоосон хуудас хараад дараа нь үсэрнэ, мөн
 * хөтчийн түүхэнд хэрэггүй мөр үлдэнэ.
 *
 * ⚠ ЭНЭ ФАЙЛ `(site)` бүлгийн ГАДНА байна: тэр бүлгийн `layout` нь
 * харанхуй брэндийн бүрхүүл (толгой, хөл) зурдаг — зөвхөн шилжих
 * зорилготой хуудсанд тэр бүхнийг рендер хийх нь дэмий.
 */
export default function HomePage() {
  redirect("/learn");
}
