"use client";

import { TrainingCenterDirectory } from "@/components/tactiq/TrainingCenterDirectory";

/**
 * СУРГАЛТЫН ТӨВҮҮДИЙН ХУУДАС.
 *
 * ⚠ УРЬД НЬ `/tournament`-ийн НЭГ ТАБ байв (`RatingPage`-тэй ижил
 * шалтгаанаар тусдаа хаяг авлаа).
 */
export default function CentersPage() {
  return <TrainingCenterDirectory />;
}
